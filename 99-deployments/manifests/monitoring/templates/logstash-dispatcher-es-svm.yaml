apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: logstash-dispatcher-es
  labels:
    release: kube-prometheus-crds  # เพื่อให้ Prometheus มองเห็น
spec:
  namespaceSelector:
    matchNames:
      - logstash-dispatcher

  selector:
    matchLabels:
      app: logstash-dispatcher-es

  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
