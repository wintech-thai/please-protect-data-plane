apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: logstash-aggregator-redis
  labels:
    release: kube-prometheus-crds  # เพื่อให้ Prometheus มองเห็น
spec:
  namespaceSelector:
    matchNames:
      - logstash-aggregator-redis

  selector:
    matchLabels:
      app: logstash-aggregator-redis

  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
