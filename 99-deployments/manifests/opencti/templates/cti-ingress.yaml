apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cti-ing
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
spec:
  tls:
    - hosts:
      - {{ .Values.domain }}
      #secretName: cti-cert
  rules:
    - host: {{ .Values.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: opencti-main-server
                port:
                  number: 80
