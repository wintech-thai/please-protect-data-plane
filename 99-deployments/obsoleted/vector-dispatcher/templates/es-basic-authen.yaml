apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: es-basic-authen
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: k8s-secret-store      # name of the SecretStore (or kind specified)
  target:
    name: es-basic-authen       # name of the k8s Secret to be created
  data:
  - secretKey: ES_USER
    remoteRef:
      key: initial-secret
      property: ES_USER

  - secretKey: ES_PASSWORD
    remoteRef:
      key: initial-secret
      property: ES_PASSWORD

