apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: censor-zeek
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'censor-zeek-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/wintech-thai/please-protect-data-plane.git
        targetRevision: "{{name}}" #Based on environment
        path: '99-deployments/manifests/censor-zeek'
        helm:
          ignoreMissingValueFiles: true
          valueFiles:
            - values.yaml
            - values-local.yaml
      destination:
        name: in-cluster
        namespace: censor-zeek
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
